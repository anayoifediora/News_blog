<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>The Gist</title>
        <link rel="stylesheet" href="/css/styles.css">
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
        <link rel="preconnect" href="https://fonts.googleapis.com">
        <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
        <link href="https://fonts.googleapis.com/css2?family=Urbanist:ital,wght@0,100..900;1,100..900&display=swap" rel="stylesheet">
    </head>
    <body>
        <header>

            <nav class="custom-navbar navbar navbar-expand-lg p-4">
                <div class="container-fluid">
                    <a class="custom-title" href="#">Gist Blog</a>
                    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                    </button>
                    <div class="collapse navbar-collapse ms-5" id="navbarSupportedContent">
                        <ul class="custom-menu navbar-nav me-auto mb-2 mb-lg-0 fw-bold">
                            <li class="nav-item ms-2">
                                <a class="nav-link text-light fs-5" aria-current="page" href="/">Home</a>
                            </li>
                            {{#if logged_in}}
                            <li class="nav-item ms-2">
                                <a class="nav-link text-light fs-5" id="logout">Logout</a>
                            </li>
                            {{else}}
                            <li class="nav-item ms-2">
                                <a class="nav-link text-light fs-5" href="/login">Login</a>
                            </li>
                            <li class="nav-item ms-2"></li>
                                <a class="nav-link text-light fs-5" href="/signup">Sign Up</a>
                            </li>
                            {{/if}}

                        </ul>
                        <div class="d-flex">
                            {{#if logged_in}}
                            <li class="nav-item me-2">
                                <a class="nav-link text-light fs-5" href="/api/users/dashboard">My Dashboard</a>
                            </li>
                            {{/if}}
                            <form class="d-flex" role="search">
                                <input class="form-control me-3" type="search" placeholder="Search for news..." aria-label="Search">
                                <button class="btn btn-outline-light" type="submit">Search</button>
                            </form>

                        </div>
                        
                    </div>
                </div>
            </nav>  
        </header>
        <main>
            {{{body}}}
        </main>
        <footer>
    
        </footer>
    </body>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    <script src="/js/logout.js"></script>
</html>